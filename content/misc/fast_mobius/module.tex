\ICPCModule{约数集合上的快速 Möbius 变换}
\ICPCMeta{适用条件}{$d$ 严格递增地存放 $m$ 的全部正约数，$ps$ 存放 $m$ 的全部不同质因子。}
\ICPCMeta{复杂度}{$O(\lvert ps\rvert\lvert d\rvert)$ 时间，除输入数组外 $O(1)$ 额外空间。}
\ICPCMeta{默认约定}{若初始 $f(x)=\sum_{x\mid y\mid m}g(y)$，变换后得到 $f(x)=g(x)$。}
\ICPCNote
\ICPCCode[快速 Möbius 变换]{code.hpp}
