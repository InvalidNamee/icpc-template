维护一次函数 $y=kx+b$ 的上包络。加入新直线时，若最后三条直线中间一条永远不可能最优，就将其弹出；交叉相乘使用 \texttt{\_\_int128}。

\texttt{add(k,b)} 要求 $k$ 单调不减。斜率相同时只保留截距更大的直线。
\texttt{query(x)} 对相邻直线的函数值二分，横坐标可按任意顺序询问。
查询前必须至少加入一条直线。若要求最小值，可把所有直线及答案同时取反。
