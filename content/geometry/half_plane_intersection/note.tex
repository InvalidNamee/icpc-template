每个半平面用有向直线 \(p+t v\) 表示，并保留直线左侧，即满足 \(cross(v,x-p)\ge0\) 的点。
算法先按方向角排序并合并同向直线，再用双端队列删除已经被新半平面排除的交点。

\texttt{run(lines)} 返回逆时针交多边形；空集、退化结果或不能形成有界多边形时返回空数组。

\begin{icpcwarning}[有界性与误差]
  若原问题可能得到无界交集，应先加入足够大的外包矩形。
  平行线、交于一点和极小面积区域对误差敏感，实际使用时应按坐标范围调整 \texttt{GEPS}。
\end{icpcwarning}
