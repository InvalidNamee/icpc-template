\texttt{polygon\_area2()} 返回有向面积的两倍；逆时针多边形为正，顺时针为负。
\texttt{point\_in\_polygon()} 使用半开区间射线法，返回 \(-1,0,1\) 分别表示外部、边界和内部。

对格点多边形，边界格点数为
\[
  B=\sum\gcd(|\Delta x|,|\Delta y|).
\]
结合 Pick 定理 \(2A=2I+B-2\)，可由面积两倍和边界格点数计算内部格点数。
