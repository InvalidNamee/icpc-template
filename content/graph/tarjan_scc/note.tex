Tarjan 算法使用 DFS 时间戳与 low-link 值求有向图的强连通分量。调用 \texttt{add(u,v)} 加入有向边，建图后调用 \texttt{run()}；\texttt{scc[u]} 是顶点 $u$ 所属的强连通分量编号。

\textbf{编号约定：}顶点统一使用 $1$-based 编号 $1,2,\ldots,n$；强连通分量编号也是 $1$-based，范围为 $1,2,\ldots,\texttt{sc}$。
