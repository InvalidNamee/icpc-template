\ICPCModule{常数查询 LCA（欧拉序 + ST 表）}
\ICPCMeta{适用条件}{静态树上大量 LCA、距离和路径判断。}
\ICPCMeta{复杂度}{预处理 $O(n\log n)$，单次查询 $O(1)$，空间 $O(n\log n)$。}
\ICPCMeta{默认约定}{点编号为 $1\sim n$，输入为树的邻接表，根默认为 $1$。}
\ICPCNote
\ICPCCode[$O(1)$ LCA]{code.hpp}
