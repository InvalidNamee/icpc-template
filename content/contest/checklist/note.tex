\begin{icpcresult}[读题与建模]
  \begin{itemize}
    \item 重读输入范围、输出格式和多组数据条件。
    \item 写清状态、转移、边界以及算法正确性依赖的条件。
    \item 用最小样例、极端样例和随机小数据手算一遍。
  \end{itemize}
\end{icpcresult}

\begin{icpcwarning}[实现检查]
  \begin{itemize}
    \item 检查函数是否有返回值
    \item 检查溢出、数组边界、未初始化变量和递归深度。
    \item 确认比较器满足严格弱序，取模前后的符号一致。
    \item 多测时清空全局容器，并重置时间戳、编号和答案。
  \end{itemize}
\end{icpcwarning}

\begin{icpcnote}[提交前]
  删除调试输出，核对文件与语言，确认复杂度覆盖最大数据范围。
  若答案依赖浮点数，重新检查误差界和输出精度。
\end{icpcnote}

