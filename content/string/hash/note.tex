字符串位置为 $0\ldots n-1$。\texttt{get(l,r)} 返回闭区间 $s[l\ldots r]$ 的双模 Hash。同一套模数和底数下，等长子串可直接比较返回值；不同长度的 Hash 没有直接可比性。

\texttt{lcp(i,j,limit)} 返回两个后缀从 $i,j$ 开始、不超过 \texttt{limit} 的最长公共前缀长度。\texttt{cmp(l1,r1,l2,r2)} 比较两个子串的字典序，返回 $-1,0,1$。

Hash 判等存在极小碰撞概率。需要完全确定的答案时使用后缀数组、后缀自动机或其他无碰撞方法；构造型对抗数据中也可随机选择底数。
