\texttt{z[i]} 是 $s[0\ldots n-1]$ 与后缀 $s[i\ldots n-1]$ 的最长公共前缀长度。

模式串 $p$ 在文本 $t$ 中匹配时，构造
\[
  u=p+\# +t,
\]
其中分隔符不出现在两个原串中。对 $u$ 求 Z 函数；文本位置 $i$ 对应 \texttt{z[|p|+1+i]}，其值至少为 $|p|$ 时，模式从 $i$ 开始出现。

判断某个后缀是否以长度 $k$ 的前缀开头，只需检查 \texttt{z[i] >= k}。枚举所有满足 $i+z[i]=n$ 的位置，还可得到同时为前缀与后缀的长度 $z[i]$。
